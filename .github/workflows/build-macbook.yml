name: Build 🏗️ Macbooks 🍏
on:
  push:
    branches:
      - main
    paths:
      - 'flake.lock'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
  id-token: write

jobs:
  build-macbook:
    name: 🍏
    runs-on: macos-latest
    strategy:
      matrix:
        flake:
          - name: apple
            type: darwin
            path: '.#darwinConfigurations.apple.config.system.build.toplevel'
          - name: apple
            type: home
            path: '.#homeConfigurations.\"henry@apple\".activationPackage'
    steps:
    - uses: actions/checkout@v6
    - uses: DeterminateSystems/determinate-nix-action@v3
      with:
        extra-conf: |
          lazy-trees = true
    - uses: DeterminateSystems/flakehub-cache-action@v2
    - name: ${{ matrix.flake.name }}-${{ matrix.flake.type }}
      run: |
        sed -i '' 's|pico8|#pico8|' home-manager/_mixins/desktop/apps/game-development/default.nix
        nix build ${{ matrix.flake.path }} -L
